package VIEW;

import javax.swing.*;


public class main_view extends JFrame {
    private JPanel painel_main;

    public main_view(){
    setContentPane(painel_main);
    setTitle("Menu Princical");
    setDefaultCloseOperation(EXIT_ON_CLOSE);
    setSize(500,400);
    }
}
